# TradingAgents-CN 项目 Git 协作说明

## 仓库信息

- 本地目录：`D:\PycharmProjects\TradingAgents-main`
- GitHub：`git@github.com:7654321x/TradingAgents-CN-main.git`
- 远端名称：`origin`
- 远端主分支：`main`
- 当前发布分支：`main`
- 当前工作版本：`1.1`（尚未完成最终提交和推送）
- 版本规则：后续发布依次使用 `1.2`、`1.3` 等递增版本，并创建对应 Git 标签。

## 首次或日常上传流程

在项目根目录使用 PowerShell 7：

```powershell
git status --short
git diff
git add --all

# reports/ 默认被忽略；只有经确认需要公开的报告才能强制加入。
git add -f -- reports/stocks/001309.SZ/2026-07-17/decision_report.md

# 提交前确认没有环境文件和数据库。
git ls-files .env market_data.db
git diff --cached --stat

python -m pytest -q tests/test_database_market_data.py tests/test_stock_decision_report.py tests/test_fund_technical_report.py tests/test_reporting.py

git commit -m "描述本次修改"
git push origin main
```

首次在新电脑上使用 SSH 时，可执行：

```powershell
ssh -T git@github.com
git remote -v
```

GitHub 返回“successfully authenticated”即表示密钥可用；GitHub 不提供 Shell，因此 SSH 命令本身返回非零退出码属于正常现象。

## 应上传的内容

- Python 源码：`tradingagents/`、`cli/`、`scripts/`；
- 数据库迁移：`alembic/`、`alembic.ini`；
- 自动化测试：`tests/`；
- 工程配置：`pyproject.toml`、`requirements.txt`、Docker 和 CI 配置；
- 不含真实密钥的配置模板：`.env.example`、`.env.enterprise.example`；
- 文档：`README.md`、`CHANGELOG.md`、`架构.md`、`项目交接文档.md`；
- 明确指定、检查过内容的决策报告。本次为：
  `reports/stocks/001309.SZ/2026-07-17/decision_report.md`。

## 必须忽略的内容

- 真实环境变量和密钥：`.env`、`.env.enterprise`、`.envrc`；
- 虚拟环境：`.venv/`、`venv/`、`env/`；
- Python/测试缓存：`__pycache__/`、`*.pyc`、`.pytest_cache/`；
- IDE 本地状态：`.idea/`；
- 本地数据库：`*.db`、`*.sqlite`、`*.sqlite3` 及 journal 文件；
- 临时与审计输出：`tmp/`、`audit_output/`；
- 数据缓存：`**/data_cache/`；
- 默认生成报告：`reports/`，只有人工确认后才允许用 `git add -f` 加入单个报告；
- 日志、覆盖率、构建产物和打包产物。

## 提交前安全检查

1. `git status --short --ignored` 检查忽略状态；
2. `git diff --cached --name-only` 检查实际上传文件；
3. 禁止提交 API Key、Token、私钥、Cookie、账号密码和个人数据；
4. 日常提交禁止使用 `git push --force`；只有仓库所有者明确要求重建主分支时，才允许使用 `--force-with-lease`；
5. 每次发布前更新 `pyproject.toml` 版本，并按 `1.1`、`1.2` 等规则累计递增；
6. 发布提交推送成功后创建对应标签，例如 `git tag -a v1.1 -m "TradingAgents-CN v1.1"`，再执行 `git push origin v1.1`；
7. 报告只上传 Markdown 等必要成果，不上传包含原始敏感数据的数据库或缓存。

## 拉取与继续开发

```powershell
git fetch origin
git switch main
git pull --ff-only origin main
```

若本地还没有主分支：

```powershell
git fetch origin
git switch --track -c main origin/main
```
