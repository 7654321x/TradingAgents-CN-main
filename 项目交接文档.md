# TradingAgents 项目交接文档

## 1. 当前代码基线

- 项目版本：`1.0`；
- Python 要求：`>=3.10`；
- 本地目录：`D:\PycharmProjects\TradingAgents-main`；
- GitHub 仓库：`git@github.com:7654321x/TradingAgents-CN-main.git`；
- 发布分支：`main`；
- 发布标签：`v1.0`；
- 详细架构说明见 `架构.md`。

当前本地项目已被确定为 TradingAgents-CN 的 `1.0` 发布基线，并按仓库所有者要求覆盖远端 `main`。后续发布版本按 `1.1`、`1.2`、`1.3` 依次递增。

## 2. 已实现内容

### 2.1 多 Agent 决策流程

- 基于 LangGraph 编排市场、基本面、新闻、情绪等分析节点；
- 包含多头/空头研究、交易员决策和激进/中性/保守风险辩论；
- 支持状态传播、条件路由、信号处理、检查点恢复与反思记忆；
- Agent 输出提供结构化 Schema 和结构化调用辅助层。

### 2.2 数据访问与标的处理

- 实现 yfinance 日线、新闻、基本资料及财务数据访问；
- 实现 Alpha Vantage、FRED、Reddit、Stocktwits、Polymarket 等数据适配；
- 提供股票代码标准化、安全路径组件和日期边界处理；
- 提供行情数据质量校验、陈旧 OHLCV 防护和无数据错误处理；
- 包含 A 股 `.SS`、`.SZ` 代码路径和单 ticker/MultiIndex 兼容逻辑。

### 2.3 技术分析和证据体系

- 基于 Pandas/stockstats 计算并组织技术指标；
- 实现股票证据引擎、证据审计和规则化决策策略；
- 实现单股决策报告生成，包含趋势、风险、数据覆盖率和改变判断的条件；
- 已保存并纳入 Git 的示例报告：
  `reports/stocks/001309.SZ/2026-07-17/decision_report.md`。

### 2.4 基金扩展

- 提供基金持仓 Universe 种子数据与 2026Q1 持仓扩展；
- 实现基金技术报告和基金持仓存储相关逻辑；
- 基金生成结果默认位于 `reports/funds/`，按安全规则不批量提交生成文件。

### 2.5 数据库存储与迁移

- 使用 SQLAlchemy 2.x 风格的 Session、模型、仓储和数据服务；
- 提供市场数据、基金持仓等存储接口；
- 包含 Alembic 基线及当前存储 Schema 迁移；
- 本地 SQLite 数据库只用于运行和验证，已从 Git 排除。

### 2.6 LLM 适配

- 包含 OpenAI、OpenAI-compatible、DeepSeek、Anthropic、Google、Azure 和 Bedrock 客户端路径；
- 实现模型目录、Provider 路由、能力声明、参数校验和 API Key 环境变量解析；
- 覆盖 reasoning effort、temperature、重试、Responses API Base URL 等配置路径；
- 真实 API Key 仅保存在本地 `.env`，不会上传。

### 2.7 工程化

- 提供 CLI、Docker、GitHub Actions 和 pytest 配置；
- 当前包含 59 个 `test_*.py` 测试文件；
- 已编写项目架构文档 `架构.md`；
- 已增加 `.agent`，记录后续 Git 上传和安全规则。

## 3. 本轮实际验证

执行命令：

```powershell
python -m pytest -q tests/test_database_market_data.py tests/test_stock_decision_report.py tests/test_fund_technical_report.py tests/test_reporting.py
```

结果：`89 passed in 6.08s`。

覆盖范围包括：

- 数据库市场数据读写；
- 股票决策报告；
- 基金技术报告；
- 通用报告输出。

SSH 验证结果：GitHub 成功识别用户 `7654321x`，远端可读取。

## 4. 已知问题与限制

### 4.1 完整测试尚无最终结果

执行 `python -m pytest -q` 时，命令在约 124 秒达到外部超时，没有返回最终统计。不能据此声明完整测试全部通过。核心子集 89 项测试已通过，不受该超时结论影响。

后续建议：在不受 120 秒限制的终端运行完整测试，并保存最终通过、失败和跳过数量。

### 4.2 远端历史已切换到新的 1.0 基线

覆盖前的远端 `main` 是另一套包含 2173 个文件的工程。本次按仓库所有者明确要求，以当前本地项目作为 `1.0` 基线覆盖主分支。旧历史仍可通过覆盖前提交 `e695002ff727b86a7ca32293b13c2a07a6f578e6` 定位，但不再是默认主线。

### 4.3 生成报告默认不被跟踪

`.gitignore` 默认忽略 `reports/`，避免大量运行结果和潜在敏感数据进入仓库。本次示例报告通过 `git add -f` 单独加入。后续新增报告也必须逐份检查、逐份加入。

### 4.4 外部数据和 LLM 测试具有环境依赖

yfinance、新闻、FRED、社交媒体、DeepSeek 等真实请求依赖网络、Provider 状态、限流、代理和本地 `.env`。单元测试通过不等于所有外部服务在任意时间均可用。

### 4.5 决策报告不是投资建议

示例报告的数据质量为 `PARTIAL`，基本面和新闻证据覆盖不足，模型置信度未经历史校准。报告是研究信号，不是自动下单指令，也不构成投资建议。

## 5. 建议的下一步

1. 在无限制终端完成全量 pytest，记录完整统计；
2. 后续功能发布前将项目版本递增为 `1.1`，并创建 `v1.1` 标签；
3. 对外部数据源和 DeepSeek 执行可重复的集成测试，记录日期、接口和错误码；
4. 完善基金持仓综合 LLM 报告的端到端验收；
5. 为 Alembic 新建数据库升级、降级和数据保持增加独立 CI 验证。

## 6. 安全交接要求

- `.env`、数据库、缓存、日志和 IDE 配置不得上传；
- 不在文档、Issue 或提交信息中粘贴 API Key；
- 推送前必须检查 `git diff --cached --name-only`；
- 日常开发不允许强制推送；只有仓库所有者再次明确要求重建历史时才允许使用 `--force-with-lease`；
- 对外分享任何决策报告前，先检查是否包含账号、密钥、原始私有数据或不应公开的投资信息。

---

## 7. 2026-07-23 暂停交接补充

### 暂停规则

- 用户要求暂停，明天继续；未创建计划任务、后台抓取或自动恢复任务。
- 当前没有运行中的测试、同步或网页抓取进程；暂停前开始的完整 pytest 已终止，不能计为通过。
- 恢复时先执行 `git status --short`，保留已有未提交改动；`.env` 内的 DeepSeek 与 Firecrawl 密钥不得写入文档、日志或提交。

### 本轮已实现

- 线上报告已采用 `sector_fund_score_v2`：核心趋势六模块为25/20/20/15/10/10，短线五模块为25/25/20/15/15；评分输入覆盖率不足80%时不输出分数。
- 当日收盘未确认、当前数据与历史基线异常间隔、成分覆盖不足时，数据质量门禁阻断线上评分；回测研究因子与线上报告隔离。
- AKShare 历史行情与MCP网页数据保持分表；MCP记录增加可用时间门禁，后续网页不会用于此前日期报告。
- 官方基金公告七日扫描会保存扫描截止日、窗口和结果；空缓存不再等同于无事件。
- AKShare 行业板块代理失败后，会尝试 Firecrawl Search；只接纳A/B级允许域名的原始网页正文，搜索摘要不入库、不评分。
- Firecrawl 原文先保存至 `mcp_web_observation`；产业网页只有同时具备数值、单位、覆盖期、发布日期和原文证据时才转为可用观测。当前提取器严格支持“集成电路产量”。
- Markdown报告已固定为15个一级章节；已用数据库中2026-07-22的020671报告验证章节数和顺序均正确。
- 新增通用基金Profile：020671显式使用半导体产业链、周期指标、事件词和来源域名；未知基金固定为“未分类主题（待核验）”，不猜测主题。

### 验证状态

- 最近一次完成的全量测试（在报告十五段/Profile最新改动之前）：`784 passed, 1 skipped, 18 warnings, 72 subtests passed`。
- 本次暂停前的专项测试：报告、数据提取、LLM边界相关 `33 passed`；相关 Ruff 与 `git diff --check` 通过。
- 暂停前重新执行的完整 pytest 被用户要求终止，未取得最终统计。明天恢复后必须重新执行 `python -m pytest -q`，以新的完整结果为准。
- Firecrawl 实测可检索国家统计局原始网页；该能力验证未把测试网页写入业务指标或评分。
- AKShare 半导体行业板块实测曾失败：`ProxyError`（东方财富代理连接被远端关闭）。失败已保存为审计记录，未进入评分。

### 当前数据状态

- 2026-07-22历史行情已验证50/50成分股价格和成交额覆盖；
- 该日期的V2线上报告仍可能不输出趋势分，因为基本面、前十大资金流、产业周期与同日事件扫描未达到80%输入覆盖阈值；这是预期门禁，不是程序故障；
- 2026-07-22以后获取的数据不能反向用于该日报告，避免未来信息穿越；
- 020671 Profile 已实际读取为“半导体”，包含8个产业链方向。

### 明日续做优先级

1. 扩展 `industry_cycle_extractor.py`：接入国家统计局、海关、工信部的多类指标，保存值、单位、覆盖期、发布日期、原文URL和证据；不完整页面不得评分。
2. 完成前十大资金流与财务字段的分层回退：AKShare失败审计、Firecrawl A/B级原文、字段结构化、覆盖率与发布日期报告。
3. 完成通用基金运行入口和主动基金降级模式：指数/ETF联接基金走完整穿透；主动基金仅使用正式净值、已披露持仓和风格暴露，并明确实时穿透不可用。
4. 进行发布前验收：完整pytest、空库/既有库Alembic、端到端报告、Ruff和文档。前三项完成并验证前，不得发布或推送`1.1`。

### 2026-07-24 恢复执行与发布候选状态

- 已恢复执行并修复“可用时间”测试的日期漂移：测试现在显式注入历史 `available_at`，不会因系统日期变化误判防未来数据门禁；
- 已完成通用基金数据库运行入口：已验证目标ETF和指数的基金走指数穿透路径；未验证关系的基金自动降级到正式净值与已披露持仓，绝不猜测主题、ETF或指数；
- 已新增财务标准化与网页证据回退：AKShare 财务行保留原始字段并标准化收入、净利润、毛利率、存货、合同负债、研发、资本开支及经营现金流；失败后重试并保存 Firecrawl A/B 原始网页；
- 已新增公司财务与产业周期严格提取器。公司收入、净利润、毛利率及产业产量/进出口/电子信息制造业指标均要求原文数值、单位、报告期、发布日期与证据；
- 已新增公司事件网页扫描：A/B 原文保存待确认，C级媒体仅写入 `news_lead`，报告中显式标记“仅作待确认线索”，不计入评分；
- 报告第2节现可显示字段级成功/失败状态与最新适用日期；
- 空 SQLite 数据库 `alembic upgrade head` 已成功升级到 `e4a8f5c13d76 (head)`；
- 当前发布版本已更新为 `1.1`；最终发布状态以远端 `main` 分支和 `v1.1` 标签为准；
- 完整测试最新结果：`797 passed, 1 skipped, 18 warnings, 72 subtests passed`；基金扩展与通用Profile严格 Ruff 检查通过。
